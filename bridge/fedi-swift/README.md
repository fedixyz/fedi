# fedi-swift

This project builds a Swift package that provides [Swift] language bindings for the 
[`fedi`] library. The Swift language bindings are created by the [`fedi-ffi`] project which is included as a module of this repository. 
